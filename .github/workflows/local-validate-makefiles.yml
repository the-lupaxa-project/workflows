name: Local Validate Makefiles

on:
  push:
    paths:
      - ".github/workflows/local-validate-makefiles.yml"
      - "Makefile"
      - "makefile"
      - "GNUmakefile"
      - "**/*.mk"
      - "**/*.make"
      - "Makefile.*"
      - "checkmake.ini"

  pull_request:
    branches:
      - "master"
    paths:
      - ".github/workflows/local-validate-makefiles.yml"
      - "Makefile"
      - "makefile"
      - "GNUmakefile"
      - "**/*.mk"
      - "**/*.make"
      - "Makefile.*"
      - "checkmake.ini"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  validate-makefiles:
    name: Validate Makefiles
    uses: ./.github/workflows/reusable-makefile-linter.yml
    with:
      check_conventions: true
      # tests/ holds intentional bad fixtures for the makefile-linter suite
      exclude_files: "^site/|^mkdocs/|^\\.makefiles/|^tests/"

  generate-workflow-summary:
    name: Generate Workflow Summary
    needs:
      - validate-makefiles
    if: always()
    uses: ./.github/workflows/reusable-workflow-summary.yml

  generate-workflow-notifications:
    name: Generate Workflow Notifications
    needs:
      - generate-workflow-summary
    if: always()
    uses: ./.github/workflows/reusable-workflow-notifier.yml
    with:
      notify_on_results: "failure,timed_out"
    secrets:
      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
