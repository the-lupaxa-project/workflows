name: Shellcheck

on:
  push:
    branches:
      - "**"
  pull_request:
  workflow_dispatch:  # add a Run Workflow button

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  shellcheck:
    name: Shellcheck
    uses: ./.github/workflows/reusable-shellcheck.yml
    secrets: inherit

  status-check:
    name: Check Jobs Status
    needs:
      - shellcheck
    if: always()
    # 🔴 IMPORTANT: this is a *job-level* uses, not under steps:
    uses: ./.github/workflows/reusable-check-jobs-status.yml
    with:
      ignore_jobs: "Check Job Status"

  slack-workflow-status:
    name: Slack Workflow Status
    needs:
      - shellcheck
    if: always()

    uses: ./.github/workflows/reusable-slack-workflow-status.yml
    with:
      notify_on_results: "failed,timed_out"
      ignore_jobs: "Check Job Status"
    secrets:
      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
