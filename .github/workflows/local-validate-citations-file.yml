name: Citation Validation

on:
  push:
    branches-ignore:
      - 'dependabot/**'
    paths:
      - 'CITATION.cff'
  pull_request:
    branches:
      - '**'
    paths:
      - 'CITATION.cff'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  validate:
    name: Citation Validation
    uses: ./.github/workflows/reusable-validate-citations-file.yml
    secrets: inherit

  status-check:
    name: Check Jobs Status
    needs:
      - validate
    if: always()
    # 🔴 IMPORTANT: this is a *job-level* uses, not under steps:
    uses: ./.github/workflows/reusable-check-jobs-status.yml
    with:
      ignore_jobs: "Check Job Status"

  slack-workflow-status:
    name: Slack Workflow Status
    needs:
      - validate
    if: always()

    uses: ./.github/workflows/reusable-slack-workflow-status.yml
    with:
      notify_on_results: "failed,timed_out"
      ignore_jobs: "Check Job Status"
    secrets:
      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
